# Neon PostgreSQL connection string
# Get it from: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional: HTTP/HTTPS proxy for scraping (e.g. http://user:pass@host:port)
# PROXY_URL=

# Optional: Max concurrent browser contexts (default 1 for anti-blocking)
# MAX_CONCURRENCY=1

# Optional: Base delay range in ms for random delays (min,max)
# DELAY_MIN_MS=2000
# DELAY_MAX_MS=5000
